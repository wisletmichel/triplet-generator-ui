export const featureFlags = Object.freeze({
    inputFileEnabled: true,
    darkMode: true
});

export const locales = Object.freeze({
    EN_US: 'English',
    FR: 'French',
});

export const configs = Object.freeze({
    TRIPLET_SERVICE: 'http://localhost:9090/api/parser'
});
